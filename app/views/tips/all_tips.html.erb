<div class="web-container">
  <div>
    <h1>Get inspired</h1>
    <p id="p-centered">Add additional tips to your lists</p>
  </div>

  <% @tips.each do |tip| %>
    <div class="tips-index-links relative">
      <%= link_to "", tip_path(tip), class:"absolute-0-0"%>
        <div class="d-flex justify-content-between align-items-center pt-3">
          <h2 class="mb-1">
          <%=  tip.title %>
          </h2>
        </div>
        <div class="list-card" style="position: relative; background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= tip.photo %>); height: 200px; margin-top: 0px;">
          <btn class="pill-category-tips-index">
            <%= tip.category %>
          </btn>
          <div style="display: flex; justify-content: space-between; position: absolute; left: 0; bottom: 0; width: 100%; padding-right: 20px; padding-left: 20px;">
          <p>By: <%= User.find(tip.user_id).first_name %></p>
          <i class="fas fa-share-alt"></i>
          </div>
        </div>
        <p class="address-tips-index mt-2" style="font-size: 16px">
          <%=  tip.address %>
        </p>
  <% end %>
</div>
